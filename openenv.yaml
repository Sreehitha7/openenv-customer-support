name: customer-support-env
description: Customer support simulation environment

version: 1.0

entrypoint:
  module: app.main
  app: app

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: easy
    description: Order tracking query
  - id: medium
    description: Refund request
  - id: hard
    description: Angry escalation
